# ORACLE-PROGRAM

**Employee salary**

CREATE TABLE EMPLOYEE_PERSONAL(
EMP_ID NUMBER(5) PRIMARY KEY,
EMP_NAME VARCHAR2(20),
ADDRESS VARCHAR2(30),
CITY VARCHAR2(20),
PIN_CODE NUMBER(6)
);
CREATE TABLE EMPLOYEE_SALARY(
EMP_ID NUMBER(5),
SALARY NUMBER(8),
CONSTRAINT FK_EMP
FOREIGN KEY (EMP_ID) REFERENCES
EMPLOYEE_PERSONAL(EMP_ID)
);
INSERT INTO EMPLOYEE_PERSONAL VALUES (101,
'ARUN','GANDHI NAGAR','COIMBATORE',641029);



INSERT INTO EMPLOYEE_PERSONAL VALUES (102,
'BALA','ANNA NAGAR','CHENNAI',641001);


INSERT INTO EMPLOYEE_PERSONAL VALUES (103,
'CHARAN','MG ROAD','SALEM',636001);



INSERT INTO EMPLOYEE_SALARY VALUES (101,18000);



INSERT INTO EMPLOYEE_SALARY VALUES (102,14000);



INSERT INTO EMPLOYEE_SALARY VALUES (103,20000);

SELECT *
 FROM EMPLOYEE_PERSONAL P, EMPLOYEE_SALARY S
 WHERE P.EMP_ID = S.EMP_ID
 AND S.SALARY>15000;
SELECT EMP_NAME, ADDRESS
 FROM EMPLOYEE_PERSONAL
  WHERE CITY = 'COIMBATORE' OR CITY = 'CHENNAI';
 SELECT EMP_NAME
FROM EMPLOYEE_PERSONAL
WHERE CITY = 'COIMBATORE'
AND (PIN_CODE = 641029 OR PIN_CODE = 641001);
SELECT *
FROM Employee_Personal
ORDER BY Emp_Name DESC;

**Student details**

CREATE TABLE STUDENT (
    REGNO NUMBER(5),
    NAME VARCHAR2(20),
    DEPARTMENT VARCHAR2(20),
    MARK NUMBER(3)
);
INSERT INTO STUDENT VALUES (101, 'ANU', 'CSE', 85);
INSERT INTO STUDENT VALUES (102, 'BALA', 'IT', 72);
INSERT INTO STUDENT VALUES (103, 'CHARAN', 'CSE', 90);
INSERT INTO STUDENT VALUES (104, 'DIVYA', 'ECE', 65);
INSERT INTO STUDENT VALUES (105, 'ESHA', 'IT', 78);
SELECT * FROM STUDENT WHERE MARK > 75;
SELECT * FROM STUDENT
WHERE DEPARTMENT = 'CSE' AND MARK > 80;
CREATE TABLE STUDENT_SPORTS (
    REGNO NUMBER(5),
    NAME VARCHAR2(20)
);
SELECT NAME FROM STUDENT
 UNION
 SELECT NAME FROM STUDENT_SPORTS;
 SELECT * FROM STUDENT
 ORDER BY NAME DESC;
SELECT DEPARTMENT, AVG(MARK)
FROM STUDENT
GROUP BY DEPARTMENT;

